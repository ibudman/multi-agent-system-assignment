OPENAI_API_KEY=
TAVILY_API_KEY=
MONGODB_URI=
MONGODB_DB=
CORS_ORIGINS=http://localhost:5173
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=

# Toggle external integrations (OpenAI, Tavily)
# 1 = use mock clients
# 0 = use real external API calls
MOCK_EXTERNAL=0

# Mock scenario selector (used only when MOCK_EXTERNAL=1)
# Supported values:
# - photography → photography learning paths
#   Example input:
#   "photography online hobby free NYC"
#
# - ux → UX design learning paths
#   Example input:
#   "ux design in person hobby paid San Francisco"
MOCK_MODE=ux